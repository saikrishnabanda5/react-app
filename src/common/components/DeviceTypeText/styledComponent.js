import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const Heading =styled.div `${tw
`flex-none`};
font-size:20px;
font-weight:bold;
`;
const ToggleStyle =styled.div `
`;
const SubHeading =styled.div `
`;

export {ToggleStyle,Heading,SubHeading}