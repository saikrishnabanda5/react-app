import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const Height = styled.div `${tw 
`overflow-y-scroll`};
height:70%
`
const AddItems = styled.div `${tw 
`flex text-sm justify-center items-center mt-64 `};
`

export {Height,AddItems}