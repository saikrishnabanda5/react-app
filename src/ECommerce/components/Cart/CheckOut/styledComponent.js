import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const CheckOutButton = styled.button `${tw 
`text-center bg-black rounded text-white pl-24 text-lg pr-24 pt-2 pb-2`}
`

export {CheckOutButton};