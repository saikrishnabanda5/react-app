import styled from '@emotion/styled';
import tw from 'tailwind.macro';

const HeaderStyle = styled.div `${tw 
`flex justify-between items-center  w-full -mt-3`
}`
const Count = styled.div `${tw 
``
}`
export {HeaderStyle,Count}